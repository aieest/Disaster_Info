<!DOCTYPE html>
<html>
<head>
  <title>App</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>

<body>
<content class="top-section">
  <img src="" alt="" class="web-logo">
  <nav>
    <ul>

      <li>
        <%= link_to 'HOME', root_path, class: "nav-btn #{'active' if action_name == 'posts' }", 'aria-current': 'page' if user_signed_in? %>
      </li>
      <li>
        <%= link_to 'POSTS', posts_path, class: "nav-btn #{'active' if action_name == 'posts' }", 'aria-current': 'page' if user_signed_in? %>
      </li>
      <li>
        <%= link_to 'MY POST', user_posts_path, class: "nav-btn #{'active' if action_name == 'posts' }", 'aria-current': 'page' if user_signed_in? %>
      </li>
      <li>
        <%= link_to 'MY COMMENTS', user_comments_path, class: "nav-btn #{'active' if action_name == 'posts' }", 'aria-current': 'page' if user_signed_in? %>
      </li>

    </ul>
  </nav>
</content>

  <% if flash[:notice] %>
    <p class="notice"><%= notice %></p>
  <% end %>
  <% if flash[:alert] %>
    <p class="alert"><%= alert %></p>
  <% end %>

  <%= yield %>
</body>
</html>
