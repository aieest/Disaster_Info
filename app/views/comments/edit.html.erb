<content class="comment-container">
<h1 class="comment-header">Title: <%= @post.title %></h1>
<%= render partial: 'form', locals: { post: @post, comment: @comment, path: post_comment_path(@post, @comment) } %>
</content>